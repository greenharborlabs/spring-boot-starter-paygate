# Non-secret diagnostic markers for low-security regression tests.
#
# These values are test sentinels, not credentials, keys, preimages, invoices, or payment hashes.
# A test may inject one into untrusted input, then must prove it is absent from every client-visible
# response and captured diagnostic. Never replace these with production, testnet, regtest, or local
# secrets. Test reports and failure messages must redact a marker as [REDACTED-MARKER].

LOW_SECURITY_MARKER_ROOT_KEY_4f7d1a29
LOW_SECURITY_MARKER_TOKEN_ID_8c3e5b10
LOW_SECURITY_MARKER_PAYMENT_HASH_61a9d2f4
LOW_SECURITY_MARKER_DESCRIPTION_c7b8e305
LOW_SECURITY_MARKER_EXTENSION_MESSAGE_12f6a9de
