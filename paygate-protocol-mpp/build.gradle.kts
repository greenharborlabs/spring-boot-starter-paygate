// paygate-protocol-mpp: MPP protocol implementation — depends on paygate-api only (NO paygate-core).
// Test deps (JUnit 5 + AssertJ) are inherited from the root build.

dependencies {
    api(project(":paygate-api"))
}
