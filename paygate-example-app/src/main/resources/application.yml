spring:
  application:
    name: paygate-example-app

server:
  port: 8080

paygate:
  enabled: true
  service-name: example-api
  protocols:
    mpp:
      challenge-binding-secret: ${PAYGATE_MPP_SECRET:}
